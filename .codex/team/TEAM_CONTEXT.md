# AI星座搭子团队上下文

更新日期：2026-08-02

## 长期角色

- CEO / 交付总协调：理解用户意图、冻结范围、分配任务、处理冲突、验收和最终汇报；不承担主要编码。
- 产品负责人：维护目标用户、价值主张、版本取舍、指标口径和所有者验收。后续优先恢复既有负责人；只有确实无法恢复时才更换运行实例，并先加载本目录与产品决策文档，明确“运行实例已更换、角色理解已恢复”。
- 技术负责人：本项目唯一主要代码写入者，负责方案、实现、测试、构建和运行证据。后续实现与修复优先继续既有负责人。

市场增长与 UX 角色在 V0.2 是临时独立调研顾问，不是长期负责人；仍有效的结论已进入产品决策与产品上下文。

## 协作规则

1. 产品负责人先冻结目标用户、唯一价值闭环、本期做与不做、可观察验收标准。
2. 技术负责人按冻结范围写入，保留已可用路径，并返回 Context delta。
3. CEO 只读复核真实实现和运行证据；问题定向退回原技术负责人。
4. 产品负责人做所有者复验；未通过的问题仍由原技术负责人修复。
5. 重大阶段把长期事实、决策、验收、风险和下一步沉淀到本目录，禁止保存完整对话、思维链、临时身份、原始日志或秘密。
6. 最终外部验收组如被启用，必须由三名全新、临时、只读的审计员分别检查产品价值与范围、技术正确性与测试、UX 与传播体验；不替代长期角色、不写代码、不直接改本目录。
7. 所有源码、配置、文档与团队档案变更都必须通过 Git 留痕：一个可回退目的对应一个清晰提交，交付和部署证据必须绑定具体提交 SHA；未提交的工作不得表述为已交付。
8. 开发期保留独立分支，正式公开版本在验收通过后进入 `main` 并创建语义化版本标签；禁止把模型密钥、限流盐、原始设备标识、原始 IP、聊天正文或本地环境文件提交到 Git。

## 当前阶段

- 当前 Git 分支为 `codex/v0.1`，当前已验证候选提交为 `5070ddd`；工作树在本次治理更新前为 clean。GitHub 远程与正式发布标签尚未建立，因此不能宣称已完成开源发布。
- V0.2 本地产品、指标和开源发布材料已于 2026-08-01 完成。
- 三名临时、只读外部审计员完成最终验收后提出 4 项 P1：默契度归属、双声道分享附件、访客限额身份和 OG origin 信任链；原技术负责人修复，CEO 定向复核，原产品负责人最终复验通过，当前 P0=0、P1=0。
- 本地源码已完成首次提交；Sites 版本 1 已于 2026-08-01 以仅所有者可见方式成功部署：`https://zodiac-persona-kit.clear-gnome-6249.chatgpt.site`。
- 该部署未配置模型运行时变量，在线聊天不是本阶段的生产验收项；测试、盲选、结果、人格导出和互动分享不依赖模型。
- GitHub 尚未公开发布，本机缺少 GitHub CLI；没有真实留存、传播或商业数据。
- 2026-08-02 三角色完成度审计确认：12 人格、测试、直选、双声道、确认、本地续聊、分享机制和导出已完成；公开匿名访问、生产真实 AI 回复、持久统计、公开仓库与远程 CI 尚未完成。
- 2026-08-02 用户把当前优先级调整为本地先可访问、可看到、可体验；产品负责人冻结“本地可用”必须包含至少一条真实 OpenAI-compatible 回复。技术负责人确认当前没有项目级模型配置或可安全复用的 localhost 上游，聊天接口为 503 `MODEL_NOT_CONFIGURED`；匿名双声道邀请卡调用链与 22/22 聚焦测试通过，未改业务代码。
- 提交 `aea5a1c` 已通过 typecheck、40/40 单测、lint、生产构建和 4/4 SSR；产品负责人关闭工程门禁 P1，当前 P0=0、P1=1，唯一剩余 P1 是本地真实模型回复。
- 提交 `3d03825` 完成 Worker 运行时白名单桥接与仅开发期的 Wrangler 秘密加载；CEO 以项目外进程配置接入 DeepSeek 后取得 `/api/chat` 200、`personaVersion=0.1.0`、非空双鱼人格回复和有效剩余额度，本地真实聊天 P1 已关闭。
- 同轮 Hero 密度优化保持原文案与视觉气质；1440×900 标题两行、390×844 标题三行，价值说明与主 CTA 均在首屏且无横向溢出。最终门禁为 typecheck、Vitest 42/42、lint、生产构建和 SSR 4/4；本地 P0=0、P1=0。
- 冻结产品简报：`docs/V0.2_PRODUCT_DECISION.md`。
- 项目总览与命令：`.codex/PROJECT_CONTEXT.md`。

## 下一步

- 本地重启时继续由所有者通过项目外进程配置提供模型变量与私盐；不得把值写入仓库、日志或团队档案，也不得把预置回复当作真实聊天证据。
- 保持产品范围不扩张；获得所有者明确授权后把现有 Sites 访问策略切换为公开，并用未登录环境验证。
- 启用生产聊天前配置 `LLM_*`、稳定私密的 `RATE_LIMIT_SALT`、模型费用硬限制，并先完成持久存储选型与 `ZODIAC_KV` 接线。
- 在真实平台验证 Worker origin 覆盖、IP/device 双限额和第二设备分享二维码；GitHub 公开发布需安装并登录 GitHub CLI。
- 上线后等待真实受邀选择和首批完整 7 日 cohort；数据出现前不宣称传播、留存或商业模式成立。
