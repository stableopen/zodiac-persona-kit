# CEO 交付上下文

更新日期：2026-08-02

## 交付目标

把 AI星座搭子做成普通中文用户无需登录即可体验的开源 Web 产品。V0.2 唯一价值闭环是：

`测试或直选 → 同题双声道盲选 → 揭示差异 → 主动确认搭子 → 本地续聊或分享同一选择 → 朋友直接参与`

星座只作为熟悉、有趣的沟通隐喻；产品不做心理测量、命运预测或真人关系判断。

## 范围裁决

- V0.2 只验证人格表达差异、主动确认、本地续聊和互动分享。
- 不做运势、星盘、关系匹配、账户、云端聊天历史、社区、排行榜、付费、长期记忆、双模型实时比较或重型 Agent 运行时。
- 开源项目可借鉴角色卡与多角色协作机制，但不复制 AGPL、限制性社区许可证或未审计实现。

## 验收历史

- 2026-08-01：V0.2 用户闭环的 9 条冻结验收标准全部通过。
- CEO 在 390×844 页面实测六题回退与焦点、双声道盲选和揭示、确认后刷新续聊、手机聊天返回与输入、安全分享深链及 `/explore` 12 人格。
- 工程全量门禁：34/34 单元测试、类型检查、lint、生产构建、3/3 服务端渲染通过。
- CEO 独立复跑留存相关 18/18 与类型检查通过。
- 产品负责人关闭“北极星不可计算”的唯一 P1，V0.2 本地所有者验收通过。
- 临时外部验收组发现 4 项 P1；原技术负责人完成修复后，全量 40/40 单元测试、类型检查、lint、生产构建和 4/4 服务端渲染通过。
- CEO 独立复跑本轮相关 19/19、4/4 服务端渲染和类型检查；在 390×844 实走“六题全 A → 选 B 射手 → 确认 → 完整结果”，最终无默契度徽章、显示“直觉选择”、无横向溢出且控制台错误为 0。
- 原产品负责人完成最终所有者复验，结论 P0=0、P1=0；外部验收问题全部关闭。
- 2026-08-01：精确源码提交已保存为 Sites 版本 1，并成功部署为仅所有者可见的生产站；访问地址为 `https://zodiac-persona-kit.clear-gnome-6249.chatgpt.site`。
- 2026-08-02：产品与技术负责人按活动目标逐项审计，确认本地产品候选完整，但公开匿名访问、生产真实聊天与持久统计、公开仓库和远程 CI 均无完成证据；项目目标保持未完成。
- 2026-08-02 本地优先迭代：产品负责人定义真实模型非空回复为本地完整可用门槛；技术负责人确认首页与 `/explore` 200、匿名 duel 分享链正确、22/22 聚焦测试通过，但无项目级模型配置或安全可复用的 localhost 上游，`/api/chat` 为 503 `MODEL_NOT_CONFIGURED`。本轮未改业务代码。
- CEO 对提交 `aea5a1c` 运行完整门禁：typecheck、40/40 单测、lint、生产构建与 4/4 SSR 全部通过；产品负责人复验关闭工程门禁 P1，当前仅剩真实本地聊天 P1。

## 证据指针

- 产品范围：`docs/V0.2_PRODUCT_DECISION.md`
- 留存口径：`docs/V0.2_RETENTION_METRIC.md`
- 双声道与分享：`src/lib/duel.ts`、`tests/duel.test.ts`
- 匿名双声道分享卡：`src/client/share-card.ts`、`tests/share-card.test.ts`
- 本地续聊：`src/lib/local-state.ts`、`tests/local-state.test.ts`
- 匿名留存：`src/server/retention.ts`、`tests/retention.test.ts`
- 访客限额：`src/server/quota.ts`、`tests/chat-api.test.ts`
- OG origin：`worker/index.ts`、`app/layout.tsx`、`tests/rendered-html.test.mjs`
- 页面闭环：`app/components/ZodiacApp.tsx`

## 风险与边界

- 通过的是本地实现，不是公开部署或市场验证。
- 真实聊天仍依赖模型配置；系统分享兼容性尚未在真实手机环境全面验证。
- 当前本地入口可看并可体验预置闭环，但真实聊天仍缺 `LLM_BASE_URL`、`LLM_API_KEY`、`LLM_MODEL`、`RATE_LIMIT_SALT`；这些值只能通过秘密配置提供，不得写入仓库或团队档案。
- KV cohort 聚合为读后写，小流量可用，正式放量前需原子计数、事务存储或单写者聚合。
- IP/device 限额同样是读后写；共享公网 IP 可能使同一网络内用户共享访客额度，生产还依赖平台代理正确覆盖来源 IP。
- OG 绝对 origin 依赖页面请求经过当前 Worker 入口；真实平台路由仍需部署验证。
- 未满 7 日的 cohort 不得当作完整 D7 结果。
- 私有生产站当前环境变量修订为 0 且条目为空；不得把在线聊天、双限额或留存视为生产已验证。
- `ZODIAC_KV` 当前只是内部 `get/put` 接口，不是已创建的 Sites 资源；生产持久统计需要先完成存储选型与适配，不能只填写变量名。
- GitHub 公开发布被本机缺少 GitHub CLI 阻塞；不得用浏览器绕过认证流程。

## 下一步

1. 先为本地站通过项目外秘密配置提供模型变量与私盐，验证一条真实 AI 回复和刷新续聊；不增加假 AI 或 demo 回答。
2. 获得所有者明确授权后切换公开访问，并从未登录环境验证首页、`/explore` 和分享深链。
3. 完成持久存储适配后验证代理 IP 头、双限额和留存写入；安装并登录 GitHub CLI 后创建公开仓库并确认 Node 22 远程 CI 全绿。
4. 上线后等待首批完整 7 日 cohort；真实数据出现前不宣称留存、传播或商业模式成立。
